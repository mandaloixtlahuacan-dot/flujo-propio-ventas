# Whapi.cloud
WHAPI_TOKEN=
WHAPI_BASE_URL=https://gate.whapi.cloud

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Webhook auth (generate a long random string)
# Prefer header: x-bot-webhook-secret
# Fallback query: ?secret=
WEBHOOK_SECRET=

# Admin WhatsApp (MX JID user part, e.g. 521XXXXXXXXXX)
BOT_ADMIN_PHONE=5213310184790

# Supabase (service role — server only, never expose to client)
SUPABASE_URL=https://glxhkvjeyhxqedxfpswl.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Optional dashboard gate for /admin?secret=
DASHBOARD_SECRET=
